rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /empresas/{id} {
      allow read, write: if request.auth != null;
    }
    match /usuarios/{id} {
      allow read, write: if request.auth != null;
    }
    match /bitacora/{id} {
      allow read, write: if request.auth != null;
    }
  }
}
